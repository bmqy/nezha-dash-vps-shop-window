version: "1"
rules:
  - base: test
    upstream: hamster1963:main # change `wei` to the owner of upstream repo
    mergeMethod: merge # Optional, one of [none, merge, squash, rebase, hardreset], Default: none.
